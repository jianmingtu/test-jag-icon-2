APP_NAME=jag-icon2
COMPONENT_NAME=jag-icon2-common
OC_NAMESPACE=af9914

# change the OC_ENV value for each env.
OC_ENV=dev
RUNTIME=java

# Below value must be used for Gold (kamloops)
DNS=apps.gold.devops.gov.bc.ca

# Below value must be used for DR (calgary)
#DNS=apps.golddr.devops.gov.bc.ca

# The http Authorization header required for performing livenessProbe & readinessProbe in deployment config during container start.
# Fetch this value from "AUTH_HEADER" key in openshift application secret in each env.
AUTH_HEADER=****
